from src.object.points import Points

import numpy as np


dummy_point_arrays = [
    Points(np.random(1, 1)),
    Points(np.random(10, 10)),
    Points(np.random(1, 10)),
    Points(np.random(10, 1)),
]